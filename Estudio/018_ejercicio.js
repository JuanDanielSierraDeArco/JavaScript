// Inicializamos el contador en 2, primer número par
let contador = 2;

// Mientras contador sea menor o igual a 20, se ejecuta el bloque
while (contador <= 20) {
    // Mostramos el número actual
    console.log(contador);
    // Incrementamos de 2 en 2 para obtener solo números pares
    contador += 2;
}
