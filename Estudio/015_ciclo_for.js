/* sintaxis del ciclo fior
for (variable; condicion, incremento) {
    // instrucciones
}
*/

// Declaramos un array con cuatro elementos
let lista = ['eat', 'sleep', 'code', 'repeat'];

// Recorremos el array usando un bucle for
for (i = 0; i < lista.length; i++) {
    // Mostramos en consola el elemento actual
    console.log(lista[i]);
}

