let nombre = 'ana';

if (nombre === 'Juan') {
    console.log(`El nombre ${nombre} es un usuario registrado`)
} 
else if (nombre === 'ana'){
    console.log(`El nombre ${nombre} es un usuario registrado`)
}
else {
    console.log(`El nombre ${nombre} no es un usuario registrado`)
}